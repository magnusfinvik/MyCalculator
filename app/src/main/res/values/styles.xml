<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyOwnButtonStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/buttonshape</item>
        <item name="android:textColor">#000000</item>
        <item name ="android:gravity">center</item>
    </style>
    <style name="buttonStyleLandScape" parent="MyOwnButtonStyle">
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">30dp</item>
    </style>

    <style name="scientificButtonStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/scientificbuttonshape</item>
        <item name="android:textColor">#000000</item>
        <item name ="android:gravity">center</item>
    </style>

    <style name="scientificButtonStyleLandScape" parent="scientificButtonStyle">
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">30dp</item>
    </style>

    <style name="removeButtonStyle" parent="MyOwnButtonStyle">
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@android:drawable/ic_input_delete</item>
    </style>

    <style name="scientificremoveButtonStyle" parent="scientificButtonStyle">
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@android:drawable/ic_input_delete</item>
    </style>

    <style name="removeButtonStyleLandscape" parent="buttonStyleLandScape">
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@android:drawable/ic_input_delete</item>
    </style>

    <style name="scientificremoveButtonStyleLandscape" parent="scientificButtonStyleLandScape">
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@android:drawable/ic_input_delete</item>
    </style>


</resources>
